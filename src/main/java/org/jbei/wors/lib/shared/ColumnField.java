package org.jbei.wors.lib.shared;

import org.jbei.wors.lib.dto.IDataTransferModel;

/**
 * Column fields for tables used on the ice platform UI
 *
 * @author Hector Plahar
 */
public enum ColumnField implements IDataTransferModel {

    SELECTION,
    TYPE,
    PART_ID,
    CREATED,
    NAME,
    SUMMARY,
    REGISTRY_NAME,
    OWNER,
    ALIAS,
    STATUS,
    BIT_SCORE,
    E_VALUE,
    ALIGNED_BP,
    ALIGNED_IDENTITY,
    LAST_ADDED,
    LAST_VISITED,
    LABEL,
    NOTES,
    LOCATION,
    DESCRIPTION,
    COUNT,
    RELEVANCE,
    ALIGNMENT
}
